diff --git a/device/rockchip/common/overlays/rootfs/luckfox-lyra/S45usbconfig b/device/rockchip/common/overlays/rootfs/luckfox-lyra/S45usbconfig
--- device/rockchip/common/overlays/rootfs/luckfox-lyra/S45usbconfig
+++ device/rockchip/common/overlays/rootfs/luckfox-lyra/S45usbconfig
@@ -5,13 +5,8 @@
 case "$1" in
 	start)
 		if [ ! -f /etc/.usb_config ]; then
-			echo "usb_acm_en" >> /etc/.usb_config
 			echo "usb_adb_en" >> /etc/.usb_config
 		else
-			# Replace the old default from earlier images with the serial gadget.
-			if grep -q '^usb_rndis_en$' /etc/.usb_config; then
-				sed -i '/^usb_rndis_en$/d' /etc/.usb_config
-			fi
-			grep -q '^usb_acm_en$' /etc/.usb_config || \
-				echo "usb_acm_en" >> /etc/.usb_config
+			sed -i '/^usb_acm_en$/d; /^usb_rndis_en$/d' /etc/.usb_config
+			grep -q '^usb_adb_en$' /etc/.usb_config || echo "usb_adb_en" >> /etc/.usb_config
 		fi
diff --git a/device/rockchip/common/overlays/rootfs/luckfox-lyra/etc/inittab b/device/rockchip/common/overlays/rootfs/luckfox-lyra/etc/inittab
--- device/rockchip/common/overlays/rootfs/luckfox-lyra/etc/inittab
+++ device/rockchip/common/overlays/rootfs/luckfox-lyra/etc/inittab
@@ -34,4 +34,3 @@
 #::respawn:-/bin/sh # ttyS0::respawn:/sbin/getty -L ttyS0 115200 vt100 # GENERIC_SERIAL
 console::respawn:/sbin/getty -L  console 0 vt100 # GENERIC_SERIAL
-ttyGS0::respawn:/sbin/getty -L ttyGS0 115200 vt100 # USB_GADGET_SERIAL
 tty1::respawn:/sbin/getty 115200 tty1
