// A relative import lets both Cargo and editor previews find the same icons,
// including when this project is opened in a multi-folder VS Code workspace.
export { IconDisplay, IconSet } from "../vendor/lucide-slint-1.41.0/lucide.slint";
